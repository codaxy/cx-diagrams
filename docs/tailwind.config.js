const colors = require('tailwindcss/colors');

module.exports = {
   mode: 'jit',
   content: ['./app/**/*.{html,js,tsx}', './common/**/*.{html,js,tsx}'],
   theme: {
      extend: {},
   },
   plugins: [],
};
