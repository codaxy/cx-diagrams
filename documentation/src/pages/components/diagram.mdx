---
layout: ../../layouts/DocsLayout.astro
title: Diagram
---

import Split from "../../components/Split.astro";
import Prose from "../../components/Prose.astro";
import ImportPath from "../../components/ImportPathWrapper.tsx";
import ConfigTable from "../../components/ConfigTable.astro";
import CodeSnippet from "../../components/CodeSnippet.astro";
import DiagramExample from "../../diagrams/DiagramExample.tsx";
import config from "../../config/Diagram";

export const code = `<Svg class="w-auto h-full bg-white">
    <Diagram
        unitSize={32}
        showGrid
        zoom={bind("$page.view.zoom")}
        offsetX={bind("$page.view.offsetX")}
        offsetY={bind("$page.view.offsetY")}
        center
    >
        <Flow gap={1}>
            <Cell width={4} height={2}>
                <Shape stroke="red" fill="white" text="Red" />
            </Cell>
            <Cell width={4} height={2}>
                <Shape stroke="blue" fill="white" text="Blue" />
            </Cell>
        </Flow>
    </Diagram>
</Svg>
<div
    class="absolute border bottom-2 left-2 bg-white text-[10px] uppercase p-1"
    text={tpl("Zoom: {$page.view.zoom:p;0} Center ({$page.view.offsetX:n;0}, {$page.view.offsetY:n;0})")}
/>`;

<Split>
  <Prose>
    # Diagram

    <ImportPath path='import { Diagram } from "cx-diagrams"' client:load />

    The `Diagram` component is used to mark the area of the diagram and provide zooming and panning functions. It's required that all diagrams use this component as a top-level parent element.

  </Prose>
</Split>

<Split>
  <CodeSnippet code={code} class="border-b border-t h-[400px] overflow-auto" />
  <div slot="right" class="h-[500px] xl:h-full xl:border-t border-b relative">
    <DiagramExample client:load />
  </div>
</Split>

<Split>
  <Prose><h2>Configuration</h2></Prose>
  <ConfigTable config={config} />
</Split>
