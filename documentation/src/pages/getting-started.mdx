---
layout: ../layouts/DocsLayout.astro
title: Getting Started
---

import Split from "../components/Split.astro";
import Prose from "../components/Prose.astro";
import CodeSnippet from "../components/CodeSnippet.astro";
import GettingStartedExample from "../diagrams/GettingStartedExample.tsx";

export const code = `import { Diagram, Cell, Shape, Flow, StraightLine } from "cx-diagrams";

<Diagram unitSize={32} showGrid center>
    <Flow gap={1}>
        <Cell>
            <Shape text="Hello" fill="lightblue" id="hello" />
        </Cell>
        <Cell>
            <Shape text="World" fill="lightgreen" id="world" />
        </Cell>
    </Flow>
    <StraightLine from="hello" to="world" stroke="black" />
</Diagram>`;

<Split>
  <Prose>
    # Getting Started

    ## Installation

    Install the package using npm or yarn:

    ```bash
    npm install cx-diagrams
    ```

    or

    ```bash
    yarn add cx-diagrams
    ```

    **Note:** This library requires [CxJS](https://cxjs.io) as a peer dependency. If you haven't already, set up a CxJS project first.

    ## Basic Example

    Here's a simple example that creates two shapes connected by a line:
  </Prose>
</Split>

<Split>
  <CodeSnippet code={code} class="border-b border-t py-4" />
  <div slot="right" class="min-h-[200px] xl:min-h-0 xl:h-full border-t border-b relative">
    <GettingStartedExample client:load />
  </div>
</Split>

<Split>
  <Prose>
    ## Key Concepts

    - **[Diagram](/components/diagram)** - The root container that provides zooming, panning, and the coordinate system
    - **[Cell](/components/cell)** - Defines a rectangular area that participates in layout flows
    - **[Shape](/components/shape)** - Visual elements like rectangles, circles, and rhombuses
    - **[Flow](/components/flow)** - Arranges cells in a directional layout (right, down, left, up)
    - **[Lines](/lines/straight-line)** - Connect shapes by referencing their `id` attributes

    ## Next Steps

    - Explore the [Shape](/components/shape) component for styling options, tooltips, and click handlers
    - Learn about [Flow](/components/flow) layouts for automatic positioning
    - Check out the [Examples](/examples/network-diagram) for more complex diagrams
  </Prose>
</Split>
